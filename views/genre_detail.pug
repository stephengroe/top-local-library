extends layout

block content
  h1 Genre: #{genre. name}

  div
    h2 Books

    dl
      each book in genre_books
        dt
          a(href=book.url) #{book.title}
        dd #{book.summary}
      else
        p There are no books in this genre.
  